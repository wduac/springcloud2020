server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wduac/springcloud-config.git #Github上的git仓库名字
          ##搜索目录.这个目录指的是github上的目录
          search-paths:
            - springcloud-config
          username: wduac
          password: dyh991008
      ##读取分支
      label: main

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/


